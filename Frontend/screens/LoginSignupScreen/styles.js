export const LoginStyles = StyleSheet.create({
  // ... (keep existing styles and add this)
  disclaimer: {
    fontSize: Typography.caption,
    color: Colors.textLight,
    textAlign: 'center',
    marginTop: Spacing.lg,
  },
});